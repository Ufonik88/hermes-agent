# 2026-08-17 — Weekly Hermes-Agent Upstream Contribution Audit

## Outcome: NO PR this week

Every concrete finding from the past 7 days is already tracked upstream, is
intentional by design, or is off-limits per standing instructions. Forcing a
duplicate would violate the "zero duplicates" rule, so this week is a valid
no-PR outcome with a triage table and backlog.

## Step 1 — Internal audit (Aug 10-17)

- session_search: 4 sessions surfaced, all were the audit cron's own prior runs
  (Aug 10/12 re-fires) plus unrelated EntropicMem/Mnemosyne sessions. No fresh
  user-reported Hermes bug.
- Logs with ERROR/traceback in window: agent.log, gateway.log, errors.log,
  daemon_pool_patch.log, tui_gateway_crash.log.
- daemon_pool fix health: `grep -c _daemon_worker_args
  ~/.hermes/hermes-agent/tools/daemon_pool.py` = **2** (fix present on disk).
  Watchdog re-applied it most recently at 2026-08-17 06:52:21. **Healthy, not
  urgent.** Note: watchdog has re-applied the fix daily (Aug 14, 15, 16, 17) —
  bi_daily_update keeps wiping it because the py3.14 fix is still unmerged
  upstream. Local carry remains fragile.
- Live install git status: `main...origin/main [ahead 1, behind 73]`, working
  tree clean (the "ahead 1" is the expected local daemon-pool commit).
- AGENTS.md truncation: prompt_builder warned
  `Context file AGENTS.md TRUNCATED: 78606 chars exceeds limit of 20000`.
  NOTE: this audit agent's own system prompt also shows AGENTS.md truncated to
  ~18K, so even internal runs operate without ~75% of the contributor rubric.

## Step 2 — Triage table (candidates and why rejected)

| Candidate | Evidence | Verdict | Reason |
|-----------|----------|---------|--------|
| AGENTS.md silent truncation (78K > 20K cap) | agent.log 2026-08-17 10:00; prompt_builder.py:2095 | REJECT | Duplicate of open issue #50165 "[Discussion] Shipped AGENTS.md at ~70KB triggers context_file_max_chars truncation warnings" (type/docs, P3, 2026-08-03). |
| web_extract on ddgs (search-only) error | agent.log 2026-08-17 08:59 | REJECT | Covered by open PR #84104 "fix(web/ddgs): enable web_extract via the ddgs Python package" (2026-08-16) + issue #58320. Tool already returns a clear typed error. |
| vision_analyze Console Go 404 "No endpoints found that support image input" | agent.log 2026-08-17 09:36 (x2) | REJECT (tracked) | Canonical issue #51220; our evidence comment on #82534. Maintainer-tracked family; established strategy is evidence-not-fix. No PR in flight, but flagged as #1 backlog. |
| daemon_pool py3.14 AttributeError '_initializer' | tui_gateway_crash.log; repeated | REJECT | OFF-LIMITS. 4 sibling PRs open (#61224/#65182/#70873/#81118). Local fix carried by watchdog. Do not file another. |
| Gateway restart loop (exit code 75) | gateway-exit-diag.log | REJECT | Intentional by design: `RestartForceExitStatus=75` + systemd Restart=on-failure (gateway/run.py:13593-13627, issues #12875/#42675). Not a bug. |
| websockets ConnectionClosedError in shielded future | errors.log 2026-08-17 09:56 | REJECT | Benign client-disconnect noise from third-party `websockets` lib. Low value. |
| Telegram reconnect storms (api.telegram.org unreachable) | gateway.log Aug 14-16 | REJECT | Environment/network (ISP blocking Telegram); gateway degrades gracefully and retries. Not a code bug. |
| Our-script tracebacks (VaultKnox ruff/pytest, Notion/SQLite cron) | agent.log | REJECT | Our own project code, not Hermes core. Out of scope. |

## Step 3-4 — Not executed (no PR selected)

Step 2 produced no qualifying non-duplicate item, so code prep, QA, and PR
submission were skipped by design.

## Step 5 — Summary

1. **No PR this week.** All findings tracked/intentional/off-limits (see table).
2. CI: N/A (no PR). First-time-contributor gate not exercised.
3. Maintainer feedback: none at submission. Our issue #84491 remains CLOSED
   (merged as #86646 on 2026-08-15). No new triage comments this week.
4. **Top 3 backlog candidates for next week:**
   1. **Vision router capability gate (fix for #51220 / evidence on #82534).**
      Re-occurred 2026-08-17. Assess a clean, surgical capability check before
      dispatching a vision call so a text-only/image-incapable provider (Console
      Go) is never selected for image input. Highest value; scope carefully to
      avoid overlapping maintainer work, and reference #51220 in the PR.
   2. **Context-file truncation UX (issue #50165).** If upstream won't split
      AGENTS.md, improve prompt_builder to surface a loud, actionable warning
      (or auto-rotate sections) instead of silently dropping ~75% of a context
      file. Low-risk, docs-adjacent.
   3. **Monitor daemon_pool py3.14 upstream merge.** Local carry is fragile
      (watchdog re-applies daily). If a sibling PR (#61224/#65182/#70873/#81118)
      merges, verify the local patch is no longer needed. Do NOT file a new
      daemon_pool PR (standing instruction).

## daemon_pool fix-health (Step 1 result)
PRESENT (grep count 2). Last watchdog re-apply: 2026-08-17 06:52:21. Healthy.
